defaults:
  - base_mnist
  - base_burstprop
  - _self_

model_name: "mnist_burstprop"

rec:
  mode: "random_init"
  scale: 0.0
  optimiser:
    type: sgd
    lr: 0.01
    momentum: 0.0
    weight_decay: 0.0